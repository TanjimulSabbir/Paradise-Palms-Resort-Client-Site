import React from 'react';

const CodeWithTrics = () => {
    // let intervalId = setInterval(function () {
    //     const NumHashName = Number(hashName);
    //     if (NumHashName < 5) {
    //         window.location.hash = `#slide${NumHashName + 1}`;
    //     } else {
    //         window.location.hash = `#slide1`;
    //     }
    //     clearInterval(intervalId);
    // }, 5000);

};

export default CodeWithTrics;